# Graph Algorithms 🕸️
Welcome in another small project devoted to graph challanges. Let me start with important remark

### 
The core Graph class implements a graph using an adjacency list representation.
This structure is used as the canonical representation for explicit graphs (edge lists, weighted graphs, etc.).
For implicit graph problems (such as grid or matrix traversal), the graph is explored directly without building a Graph instance.